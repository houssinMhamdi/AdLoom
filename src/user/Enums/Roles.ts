export enum UerRole {
  Admin = 'admin',
  Advertiser = 'advertiser',
  VenueOwner = 'venueOwner',
}

export enum Status {
  Incomplete = 'incomplete',
  Active = 'active',
  Rejected = 'rejected',
}

export enum ProfileStatus {
  Inactive = 'inactive',
  Complete = 'complete',
  Banned = 'banned',
  Pending = 'pending',
}
